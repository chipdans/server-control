name: Promote Server Control beta 8

on:
  push:
    branches: [main]
    paths: [.github/workflows/promote-beta8.yml]

permissions:
  contents: write

jobs:
  promote:
    runs-on: ubuntu-latest
    steps:
      - name: Mark beta 8 as latest release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release edit v2.0.0-beta.8 --repo "${{ github.repository }}" --prerelease=false --latest
